#include "main.h"

/**
* _islower - checks for lowercase character
* Return: 1 if char is lowcase, otherwise 0.
*/

int _islower(int snow)
{
if (snow >= 97 && snow <= 122)
return (1);
else
return (0);
}
