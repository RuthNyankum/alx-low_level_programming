#include "main.h"

/**
* _abs -  a function that computes the absolute value of an integer.
* @t: the int in question
*
* Return: the abs of the int.
*/

int _abs(int t)
{
if (t >= 0)
return (t);
else
return (-t);
}
